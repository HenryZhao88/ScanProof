{
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "build": "npm run build --workspace frontend && node ./scripts/copy-frontend-dist.mjs",
    "dev": "npm run dev --workspace frontend"
  }
}